= javascript_include_tag "smart.info.window"
= javascript_include_tag "index.maps"

.row-fluid
  - unless current_user.nil?
    .span3
      %h4 Add Shelter
      = render "form"
    .span9
      = render :partial => "view", :locals => {:shelters => @shelters }
  - else
    .span12
      = render :partial => "view", :locals => {:shelters => @shelters }


= content_tag "div", id: "shelters", data: {shelters: Shelter.all} do
  &nbsp

#map_canvas{:style =>"height: 600px; width: 600px" }
